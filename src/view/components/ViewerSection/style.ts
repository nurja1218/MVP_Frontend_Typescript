import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    section: {
        width: '100%',
        height: 'auto',
        overflow: 'scroll',
    },
}));
